{% extends 'base.html.twig' %}

{% block title %}Post{% endblock %}

{% block body %}
<div class="col-lg-12 div-comment">
    <h1>Post</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ post.id }}</td>
            </tr>
            <tr>
                <th>Tittle</th>
                <td>{{ post.tittle }}</td>
            </tr>
            <tr>
                <th>Brief</th>
                <td>{{ post.brief }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ post.content }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td><img src="{{asset('uploads/images')}}/{{ post.image }}" style="max-width: 100px;height: auto;max-height: 100px;text-align: center"></td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ post.createdAt ? post.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" style="float: left;" href="{{ path('post_index') }}">back to list</a>

    <a class="btn btn-primary" style="float: right;" href="{{ path('post_edit', {'id': post.id}) }}">edit</a>
</div>
{% endblock %}
